{
    "name": "mongo-app",
    "version": "0.0.1",
    "private": true,
    "scripts": {
        "start": "NODE_ENV=prod node index",
        "dev": "NODE_ENV=dev nodemon --exec 'node index'",
        "test": "NODE_ENV=test nodemon --exec 'mocha --recursive'"
    },
    "dependencies": {
        "bcrypt": "^5.0.1",
        "body-parser": "^1.19.0",
        "cors": "^2.8.5",
        "express": "^4.17.1",
        "jsonwebtoken": "^8.5.1",
        "mocha": "^9.1.3",
        "mongoose": "^6.0.12",
        "mongoose-autopopulate": "^0.16.0",
        "nodemon": "^2.0.15",
        "supertest": "^6.1.6"
    }
}
